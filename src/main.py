""" main """

if __name__ == "__main__":
    """
    CREATE EVENT
    args = args
    event = Event('date', 'title', 'description', 'rating' 'user', 'group')
    handler = CSV(event)
    handler.create()

    READ EVENT
    args = args
    handler = SCV()
    result = handler.read('event_title', 'cera mettings')


    """
    print("hello")
